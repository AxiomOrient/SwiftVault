# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*

# Thumbnails (Windows, but good practice)
Thumbs.db
ehthumbs.db

# Files that might appear in the root of a volume/share
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk


# Xcode
## Build generated
build/
DerivedData/

## Various Xcode files
*.hmap
*.ipa
*.dSYM.zip
*.dSYM
*.profdata

## User-specific Xcode data
# Covers user-specific files within .xcodeproj and .xcworkspace bundles
*.xcuserdatad
xcuserdata/
*.user
*.mode1v3
*.mode2v3
*.perspectivev3
*.xccheckout
*.xcscmblueprint

# User-specific workspace settings. Shared settings should be committed.
# MyWorkspace.xcworkspace/xcshareddata/WorkspaceSettings.xcsettings
# User-specific settings are typically in MyWorkspace.xcworkspace/xcuserdata/

# Generated by Xcode
*.moved-aside


# Swift Package Manager
# For a library, Package.swift is the source of truth.
.build/
Packages/
.swiftpm/ # Covers .swiftpm/configuration/registries.json and .swiftpm/xcode/*
*.xcodeproj # Generated Xcode projects should be ignored for SPM libraries


# Dependency Managers
## CocoaPods
# Used if the library has an example app using CocoaPods, or if consumers use it.
Pods/
# Podfile.lock for an example app is usually committed.
# !Podfile.lock

## Carthage
# Used if the library supports Carthage or has an example app using it.
Carthage/Build/
Carthage/Checkouts/


# Secrets & Credentials
.netrc # Network credentials
*.env
.env.*
!.env.example
*.keychain


# Testing
# LLVM code coverage files
*.gcno
*.gcda
*.profraw


# Fastlane
# Fastlane session files can contain sensitive data
*.session


# Optional: SwiftLint
# If you use SwiftLint, its cache should be ignored. Configuration (e.g. .swiftlint.yml) should be committed.
# .swiftlint_cache/
